require "spec_helper"

RSpec.describe Geocoder::Failover do
  it "has a version number" do
    expect(Geocoder::Failover::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
